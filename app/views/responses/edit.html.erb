<%= form_for @response, :url => message_response_path(@response), :html => { :method => :put } do |f| %>
	
    <%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update" %>
<% end %>